/**
 * The office number shown on the signed-out surfaces (sign-in footer, PIN
 * reset). Single constant so the recovery screens and the login footer can
 * never drift — a worker who can't get in must always see the same number.
 */
export const OFFICE_PHONE = "0421 558 902";
