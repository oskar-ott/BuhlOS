# Existing legacy ignores
.vercel
node_modules
.env.local
.env*.local

# Next.js
.next/
out/
next-env.d.ts.bak

# TypeScript
*.tsbuildinfo

# Testing
coverage/
playwright-report/
test-results/
.playwright/
playwright/.cache/
# Pre-purge jobs.json backups from scripts/qa/purge-smoke-jobs.js (live data — never commit)
.qa-backups/

# Editor / OS
.DS_Store
.idea/
.vscode/

# ESLint cache
.eslintcache

# Claude Code (local agent worktrees — never commit)
.claude/worktrees/
